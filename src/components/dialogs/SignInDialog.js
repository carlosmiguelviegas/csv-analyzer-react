import styles from './SignInDialog.module.css';

const SignInDialog = () => {

  return (
    <section className={styles.modal}>
      <p>sign in modal</p>
    </section>
  );

};

export default SignInDialog;
